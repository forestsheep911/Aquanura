# ===========================================
# Kintone Plugin Deploy Tools - Environment Configuration
# Copy this file to .env and fill in your actual values
# ===========================================

# === Basic Settings ===
NODE_ENV=development
# DEV_MODE：instant（即时重建）/ lazy（懒编译）/ true|false（兼容旧值）
DEV_MODE=instant
# 懒编译静默窗口（仅 DEV_MODE=lazy 时生效，支持 45s/2m/1h 等写法）
DEV_LAZY_WINDOW=60s
VITE_LOG_LEVEL=info
VITE_HOST=127.0.0.1
VITE_PORT=5173

# === HTTPS & 热重载配置 ===
# 为HTTPS证书添加额外的域名（逗号分隔）
# 默认已包含：localhost, 127.0.0.1, ::1
# 使用场景：
#   - 通过自定义域名访问（如 dev.local）
#   - 局域网其他设备访问（如 192.168.1.100）
# 示例：DEV_HTTPS_DOMAINS=dev.local,192.168.1.100
DEV_HTTPS_DOMAINS=

# 是否在开发服务器启动时自动上传代理插件到Kintone
# true（默认）：完全自动化，每次启动自动上传（推荐日常开发使用）
# false：需要手动上传一次代理插件，之后热重载生效
# 注意：需要配置下方的 Kintone 凭证
DEV_UPLOAD=true

# === Logging Settings ===
# Enable local JSONL logging to logistics/log/dev.log
DEV_LOCAL_LOG_ENABLED=true
# Optional: Custom log directory (default: logistics/log)
# DEV_LOG_DIR=logistics/log
# Set to true when you don't need terminal prompts
# QUIET=false

# === Plugin Path Configuration ===
PLUGIN_ROOT=./plugin
# Override auto-upload or build output plugin package path with these variables
# PLUGIN_FILE_PATH=./plugin/dist/plugin-dev.zip
# PLUGIN_ZIP=./plugin/dist/plugin.zip

# === Kintone Development Environment ===
KINTONE_DEV_BASE_URL=https://your-dev-domain.cybozu.com/
KINTONE_DEV_USERNAME=your-dev-username
KINTONE_DEV_PASSWORD=your-dev-password

# === Kintone Production Environment ===
KINTONE_PROD_BASE_URL=https://your-prod-domain.cybozu.com/
KINTONE_PROD_USERNAME=your-prod-username
KINTONE_PROD_PASSWORD=your-prod-password
